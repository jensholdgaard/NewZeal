# CI build workflow that compiles the project and uploads artifacts for testing.
# Download artifacts from the Actions tab after a successful build.

name: Build

on:
  push:
    branches-ignore:
      - 'dependabot/**'
  pull_request:

env:
  SOLUTION_FILE_PATH: ./Zeal.sln

permissions:
  contents: read

jobs:
  build:
    name: Build and upload artifacts
    runs-on: windows-2022
    outputs:
      artifact: zeal-${{ steps.zeal_version.outputs.version }}-${{ steps.parse_git.outputs.short_hash }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@v3

      - name: Parse the git short hash
        id: parse_git
        shell: bash
        run: |
          SHORT_HASH=$(git rev-parse --short "$GITHUB_SHA")
          echo short_hash=$SHORT_HASH >> $GITHUB_OUTPUT

      - name: Extract ZEAL_VERSION from source
        id: zeal_version
        shell: bash
        env:
          SED_PARAMS: 's/^[^"]*"\([^"]*\)".*/\1/'
        run: |
          ZEAL_VERSION=$(grep '#define ZEAL_VERSION' Zeal/zeal.h | sed "$SED_PARAMS")
          echo version=v$ZEAL_VERSION >> $GITHUB_OUTPUT

      # The exporter hand-builds OTLP JSON, so validate our payload shapes against the official
      # opentelemetry-proto schema — catches "valid JSON, invalid OTLP" before it can ship.
      - name: Validate OTLP payload shapes against the official schema
        shell: bash
        run: |
          python -m pip install --quiet opentelemetry-proto
          python ci/otlp_validate.py ci/fixtures/*.json

      - name: Build
        env:
          ZEAL_BUILD_VERSION: ${{ steps.parse_git.outputs.short_hash }}
        run: |
          msbuild /m /p:Configuration=Release /p:Platform=x86 /p:zeal_build_version=$env:ZEAL_BUILD_VERSION /p:LanguageStandard=stdcpp20 $env:SOLUTION_FILE_PATH /p:ExportHeader="" /p:ScanDependencies=""

      - name: Upload build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: zeal-${{ steps.zeal_version.outputs.version }}-${{ steps.parse_git.outputs.short_hash }}
          path: |
            Release/Zeal.asi
            Release/Zeal.pdb
            Zeal/uifiles
            README.md

  # Guild members install Zeal.asi from the otlp-preview release via the PowerShell installer, which
  # verifies it against Zeal.asi.sha256. Publishing both from the same build is what makes that
  # verification meaningful — a hash computed later, from the already-published file, would only ever
  # confirm the download was not truncated.
  publish-preview:
    name: Publish otlp-preview release
    needs: build
    if: github.ref == 'refs/heads/otlp-exporter'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download the build
        uses: actions/download-artifact@v7
        with:
          name: ${{ needs.build.outputs.artifact }}
          path: dist

      - name: Hash and publish
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          cd dist/Release
          sha256sum Zeal.asi | awk '{print $1"  Zeal.asi"}' > Zeal.asi.sha256
          cat Zeal.asi.sha256
          gh release view otlp-preview -R "$REPO" >/dev/null 2>&1 || \
            gh release create otlp-preview -R "$REPO" --prerelease \
              --title "OTLP preview" \
              --notes "Rolling build of the OTLP exporter branch, installed by the guild PowerShell installer."
          gh release upload otlp-preview Zeal.asi Zeal.asi.sha256 --clobber -R "$REPO"
